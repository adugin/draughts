# DRAUGHTS — контекст проекта для Claude

## Цель проекта

Переписать игру **русские шашки** с **Borland Pascal 7.0** (~1998–2000) на **Python 3.12+ / PyQt6**.
Полный технический разбор оригинала: **[ANALYSIS.md](ANALYSIS.md)**
Экранная раскладка (640×480): **[SCREEN_LAYOUT.md](SCREEN_LAYOUT.md)**

---

## Команды разработки

```bash
# Запуск игры
python main.py

# Тесты (запускать перед коммитом)
python -m pytest tests/ -q

# Линтер + форматтер (ruff)
ruff check draughts/ tests/          # проверка
ruff check --fix draughts/ tests/    # автоисправление
ruff format draughts/ tests/         # форматирование

# Проверка типов (mypy)
mypy draughts/ --ignore-missing-imports
```

Конфигурация ruff и mypy — в `pyproject.toml`.

---

## Git-workflow

- Рабочая ветка: **`dev`** — все изменения коммитим и пушим сюда
- Ветка **`master`** защищена: **никогда не пушить в master** без явного указания
- Слияние `dev → master` — только по явному запросу пользователя
- Коммит после каждого целостного блока, push сразу в dev

---

## Принятые решения

- **Фреймворк**: PyQt6 (единственная зависимость)
- **UI**: современный (стандартные виджеты), компоновка близка к оригиналу, без DOS-стиля
- **Окно**: изменяемый размер с пропорциональным масштабированием
- **Язык интерфейса**: только русский
- **Звук**: процедурно сгенерированные WAV, через QMediaPlayer
- **Форматы**: JSON (без совместимости со старыми .sav)
- **Автор оригинала**: упоминание в заставке и «О программе»
- `IZVRAT.PAS` (10×10) — только после завершения 8×8 и по отдельному запросу

---

## Ключевые факты о коде

- Доска: `grid[y][x]`, символы `'b'/'B'/'w'/'W'/'n'`, индексация 1..8
- Тёмные клетки: `x % 2 != y % 2` → 32 активные клетки
- `y=1` — верх (чёрные), `y=8` — низ (белые); нотация `(1,1)=a8`
- Русские шашки: обязательное взятие, множественные прыжки, дамки на любое расстояние
- ИИ играет чёрными: `SeeBeat()` → `Combination()` → `Action()` (приоритет)
- Подробности в [ANALYSIS.md](ANALYSIS.md)

---

## Отчёт по выполнению задачи

После каждой задачи — отчёт:

1. **Задача:** краткая формулировка
2. **Статус:** успешно / частично / не выполнено
3. **Что сделано:** перечень действий и изменений
4. **Изменения:** файлы созданы / изменены / удалены
5. **Примечания:** предупреждения, рекомендации (если есть)

---

## Что НЕ делать без явного запроса

- Улучшать ИИ (алгоритм намеренно простой — это порт оригинала)
- Добавлять режим «человек vs человек» или сетевую игру
- Менять правила шашек
- Переводить интерфейс
